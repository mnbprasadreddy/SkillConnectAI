const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { try { const solved = await prisma.submission.findMany({ where: { userId: 1, result: 'accepted' }, select: { problemId: true }, distinct: ['problemId'] }); console.log('SUCCESS:', solved); } catch(e) { console.error('ERROR:', e.message); } finally { await prisma.$disconnect(); } } main();
